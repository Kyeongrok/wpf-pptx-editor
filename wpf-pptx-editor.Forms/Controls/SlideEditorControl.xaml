<UserControl
    x:Class="wpf_pptx_editor.Forms.Controls.SlideEditorControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#5A5A5A">
        <Viewbox Margin="20" Stretch="Uniform">
            <Border
                x:Name="SlideBorder"
                Width="960"
                Height="540"
                Background="White">
                <Border.Effect>
                    <DropShadowEffect
                        BlurRadius="20"
                        Direction="315"
                        Opacity="0.5"
                        ShadowDepth="6"
                        Color="Black" />
                </Border.Effect>
                <Grid ClipToBounds="True">
                    <!--  도형 레이어  -->
                    <Canvas
                        x:Name="ShapeCanvas"
                        Width="960"
                        Height="540"
                        Background="Transparent"
                        Cursor="{Binding CanvasCursor}"
                        MouseLeftButtonDown="Canvas_MouseDown"
                        MouseLeftButtonUp="Canvas_MouseUp"
                        MouseMove="Canvas_MouseMove" />
                    <!--  그리기 미리보기  -->
                    <Canvas
                        x:Name="PreviewCanvas"
                        Width="960"
                        Height="540"
                        IsHitTestVisible="False" />
                    <!--  선택 핸들  -->
                    <Canvas
                        x:Name="HandleCanvas"
                        Width="960"
                        Height="540"
                        IsHitTestVisible="False" />
                </Grid>
            </Border>
        </Viewbox>
    </Grid>
</UserControl>
